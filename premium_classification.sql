-- ============================================================
--  تصنيف المحتوى المجاني / المميّز في المكتبة · منصة SAC · SVT prof
--  الصق هذا الكود في: Supabase → SQL Editor → New query → Run
-- ============================================================

-- القاعدة: كل ما ليس «كتب ومجلّات» يصبح مجانيًا، و«كتب ومجلّات» مميّزة
update public.files
set is_free = (category <> 'كتب');

-- (اختياري) تحقّق سريع من النتيجة:
-- select category, is_free, count(*) from public.files group by category, is_free order by category;

-- ============================================================
--  ✅ انتهى.
--  - فئة «كتب» → is_free = false (مميّز، يظهر مقفلًا لغير المشتركين)
--  - باقي الفئات (مذكرات، عروض، فروض، وثائق...) → is_free = true (مجاني)
-- ============================================================
